# EcoLearn Backend Configuration
# Copy to .env and update with your production database credentials

PORT=3001
JWT_SECRET=your_super_secret_key_here

# MySQL Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=ecolearn

# Optional: if using a connection string instead of individual params
# DATABASE_URL=mysql://user:pass@host:port/db
